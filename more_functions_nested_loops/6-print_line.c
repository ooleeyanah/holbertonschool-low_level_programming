#include "main.h"
/**
 * print_line - prints a line based on input
 * @n: times the line is printed
 * Return: no return
 */
void print_line(int n)
{
int i;
for (i = 0; i < n; i++)
{
_putchar(95);
}
_putchar('\n');
}
