#include "main.h"
/**
 * mul - Multiplies two integers and returns
 * the result.
 *
 * @a: number one.
 * @b: number two.
 *
 * Return: Multiplication of number one and number two.
 */
int mul(int a, int b)
{
return (a * b);
}
